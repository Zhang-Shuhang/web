// @Summary 获得中文的拼音
// @Produce application/json
// @Produce application/json
// @Param {}
// @Router /py/convert [post]
export const randInt = (max) => {
    return Math.floor(Math.random() * Math.floor(max));
}